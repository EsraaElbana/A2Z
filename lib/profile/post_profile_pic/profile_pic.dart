import 'dart:io';
import 'package:flutter/material.dart';

class profilePicpost extends StatefulWidget {
  const profilePicpost({super.key});

  @override
  State<profilePicpost> createState() => _profilePicpostState();
}

class _profilePicpostState extends State<profilePicpost> {
  File ?image;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        //child: cub,
      ),
    );
  }
}
